// Placeholder para la conexión con Firebase Admin
import admin from 'firebase-admin';

declare const console: { log(message?: any, ...optionalParams: any[]): void };

// Aquí luego inicializaremos con las credenciales
console.log('Firebase config cargado');